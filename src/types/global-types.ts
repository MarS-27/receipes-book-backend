export type TToken = {
  token: string;
};

export type TMessage = {
  message: string;
};

export type TLogin = {
  id: number;
  email: string;
  role: string;
  userName: string;
  token: string;
};
